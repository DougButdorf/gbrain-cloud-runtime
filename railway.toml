[build]
builder = "DOCKERFILE"
dockerfilePath = "infra/gbrain-cloud-runtime/Dockerfile"

[deploy]
startCommand = "/app/entrypoint.sh"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
